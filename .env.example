# This file is for local development environments.
# For production, use a secure method for managing environment variables.
# DO NOT COMMIT THIS FILE with real secrets.

# Generate a secure, random string (e.g., using openssl rand -hex 32)
JWT_SECRET=your_super_secret_jwt_key_that_is_at_least_32_characters_long

# Standard MongoDB connection string
MONGO_URI=mongodb://mongo:27017/rakhsha

# The server's private key in PEM format.
# This MUST be kept secret. Load it from a secure vault or environment variable.
# Example format (replace with your actual key):
# SERVER_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
SERVER_PRIVATE_KEY=""

# The public address of this server, used for mixnet routing.
SERVER_ADDRESS="http://localhost:3000"
