const crypto = require('crypto');

// This file is intentionally left sparse.
// The previous server-side encryption functions have been removed
// as all encryption is now handled end-to-end on the client.
// This module remains for any potential future server-side crypto needs.

module.exports = {};
